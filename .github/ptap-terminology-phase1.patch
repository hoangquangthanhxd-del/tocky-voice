diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@
 mod state;
+mod terminology;
 
 use tauri::Manager;
diff --git a/src-tauri/src/settings/mod.rs b/src-tauri/src/settings/mod.rs
--- a/src-tauri/src/settings/mod.rs
+++ b/src-tauri/src/settings/mod.rs
@@
 pub struct HistorySettings {
     pub enabled: bool,
@@
     pub audio_retention_days: i64,
 }
+
+#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
+pub struct TerminologyEntry {
+    /// The exact text that must reach the target application.
+    pub canonical: String,
+    /// Forms the STT service commonly returns for the same spoken term.
+    pub aliases: Vec<String>,
+    /// Lets users temporarily disable a mapping without deleting it.
+    #[serde(default = "default_true")]
+    pub enabled: bool,
+}
+
+#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
+pub struct TerminologySettings {
+    #[serde(default = "default_true")]
+    pub enabled: bool,
+    #[serde(default = "default_true")]
+    pub send_to_stt: bool,
+    #[serde(default)]
+    pub entries: Vec<TerminologyEntry>,
+}
+
+impl Default for TerminologySettings {
+    fn default() -> Self {
+        Self {
+            enabled: true,
+            send_to_stt: true,
+            entries: Vec::new(),
+        }
+    }
+}
@@
 pub struct AppSettings {
     pub stt: SttSettings,
+    /// Domain vocabulary and deterministic transcript replacements.
+    #[serde(default)]
+    pub terminology: TerminologySettings,
     pub llm: LlmSettings,
@@
 fn default_true() -> bool {
     true
 }
diff --git a/src-tauri/src/settings/defaults.rs b/src-tauri/src/settings/defaults.rs
--- a/src-tauri/src/settings/defaults.rs
+++ b/src-tauri/src/settings/defaults.rs
@@
         stt: SttSettings {
             provider: SttProviderKind::Soniox,
@@
             language_hints: vec!["vi".into(), "en".into()],
         },
+        terminology: TerminologySettings::default(),
         llm: LlmSettings {
diff --git a/src-tauri/src/terminology.rs b/src-tauri/src/terminology.rs
new file mode 100644
--- /dev/null
+++ b/src-tauri/src/terminology.rs
@@
+//! Deterministic terminology mapping for domain-specific dictation.
+//!
+//! Mapping is deliberately local and deterministic. The speech provider and optional
+//! LLM are both probabilistic; this final layer is what guarantees that a known alias
+//! such as "7 pk 2604" can become the exact canonical code "7PK2604" before paste.
+
+use crate::settings::TerminologySettings;
+use std::collections::HashSet;
+
+#[derive(Debug)]
+struct Candidate<'a> {
+    canonical: &'a str,
+    units: Vec<String>,
+}
+
+fn lower_unit(c: char) -> String {
+    c.to_lowercase().collect()
+}
+
+fn is_word_char(c: char) -> bool {
+    c.is_alphanumeric() || c == '_'
+}
+
+fn boundary_ok(chars: &[char], start: usize, end: usize) -> bool {
+    let left_ok = start == 0 || !is_word_char(chars[start - 1]);
+    let right_ok = end == chars.len() || !is_word_char(chars[end]);
+    left_ok && right_ok
+}
+
+/// Applies aliases in one pass so mappings cannot cascade into each other.
+///
+/// Longest aliases win. Replacement happens only at word boundaries, preventing a
+/// short alias like `api` from changing a larger word that merely contains it.
+pub fn apply(text: &str, settings: &TerminologySettings) -> String {
+    if !settings.enabled || settings.entries.is_empty() || text.is_empty() {
+        return text.to_string();
+    }
+
+    let mut candidates = Vec::new();
+    for entry in settings.entries.iter().filter(|entry| entry.enabled) {
+        let canonical = entry.canonical.trim();
+        if canonical.is_empty() {
+            continue;
+        }
+
+        // Canonical itself participates too, which restores exact casing/spacing if an
+        // AI cleanup pass changes it while keeping the same letters.
+        for alias in std::iter::once(canonical).chain(entry.aliases.iter().map(String::as_str)) {
+            let alias = alias.trim();
+            if alias.is_empty() {
+                continue;
+            }
+            candidates.push(Candidate {
+                canonical,
+                units: alias.chars().map(lower_unit).collect(),
+            });
+        }
+    }
+
+    candidates.sort_by(|a, b| b.units.len().cmp(&a.units.len()));
+    if candidates.is_empty() {
+        return text.to_string();
+    }
+
+    let chars: Vec<char> = text.chars().collect();
+    let lowered: Vec<String> = chars.iter().copied().map(lower_unit).collect();
+    let mut out = String::with_capacity(text.len());
+    let mut i = 0;
+
+    while i < chars.len() {
+        let matched = candidates.iter().find(|candidate| {
+            let end = i + candidate.units.len();
+            end <= chars.len()
+                && boundary_ok(&chars, i, end)
+                && lowered[i..end] == candidate.units[..]
+        });
+
+        if let Some(candidate) = matched {
+            out.push_str(candidate.canonical);
+            i += candidate.units.len();
+        } else {
+            out.push(chars[i]);
+            i += 1;
+        }
+    }
+
+    out
+}
+
+/// Canonical terms sent to speech providers as vocabulary hints.
+///
+/// Aliases are intentionally not sent: they describe recognition mistakes, while the
+/// provider should be encouraged to emit the canonical spelling. Provider APIs cap
+/// terminology lists, so callers choose the limit (100 for Deepgram Nova-2).
+pub fn stt_terms(settings: &TerminologySettings, limit: usize) -> Vec<String> {
+    if !settings.enabled || !settings.send_to_stt || limit == 0 {
+        return Vec::new();
+    }
+
+    let mut seen = HashSet::new();
+    let mut result = Vec::new();
+    for entry in settings.entries.iter().filter(|entry| entry.enabled) {
+        let canonical = entry.canonical.trim();
+        if canonical.is_empty() {
+            continue;
+        }
+        let key = canonical.to_lowercase();
+        if seen.insert(key) {
+            result.push(canonical.to_string());
+            if result.len() == limit {
+                break;
+            }
+        }
+    }
+    result
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+    use crate::settings::{TerminologyEntry, TerminologySettings};
+
+    fn dictionary(entries: Vec<TerminologyEntry>) -> TerminologySettings {
+        TerminologySettings {
+            enabled: true,
+            send_to_stt: true,
+            entries,
+        }
+    }
+
+    fn entry(canonical: &str, aliases: &[&str]) -> TerminologyEntry {
+        TerminologyEntry {
+            canonical: canonical.into(),
+            aliases: aliases.iter().map(|value| (*value).into()).collect(),
+            enabled: true,
+        }
+    }
+
+    #[test]
+    fn maps_a_spoken_part_code_to_exact_canonical_text() {
+        let settings = dictionary(vec![entry(
+            "7PK2604",
+            &["7 pk 2604", "bảy pê ka hai sáu không bốn"],
+        )]);
+        assert_eq!(
+            apply("lấy dây 7 pk 2604 giúp tôi", &settings),
+            "lấy dây 7PK2604 giúp tôi"
+        );
+    }
+
+    #[test]
+    fn longest_alias_wins() {
+        let settings = dictionary(vec![
+            entry("PK", &["pê ka"]),
+            entry("7PK2604", &["bảy pê ka hai sáu không bốn"]),
+        ]);
+        assert_eq!(
+            apply("bảy pê ka hai sáu không bốn", &settings),
+            "7PK2604"
+        );
+    }
+
+    #[test]
+    fn does_not_replace_inside_a_larger_word() {
+        let settings = dictionary(vec![entry("API", &["api"])]);
+        assert_eq!(apply("rapid api test", &settings), "rapid API test");
+    }
+
+    #[test]
+    fn disabled_entries_are_ignored() {
+        let mut disabled = entry("4G92", &["4 g 92"]);
+        disabled.enabled = false;
+        let settings = dictionary(vec![disabled]);
+        assert_eq!(apply("4 g 92", &settings), "4 g 92");
+    }
+
+    #[test]
+    fn stt_terms_are_unique_and_bounded() {
+        let settings = dictionary(vec![
+            entry("7PK2604", &[]),
+            entry("7pk2604", &[]),
+            entry("4G92", &[]),
+        ]);
+        assert_eq!(stt_terms(&settings, 1), vec!["7PK2604"]);
+    }
+}
diff --git a/src-tauri/src/session/pipeline.rs b/src-tauri/src/session/pipeline.rs
--- a/src-tauri/src/session/pipeline.rs
+++ b/src-tauri/src/session/pipeline.rs
@@
-use crate::{audio, inject};
+use crate::{audio, inject, terminology};
@@
-    let final_text = if mode.ai_cleanup {
+    // First pass gives the LLM stable domain terms instead of asking it to reason about
+    // a known STT mistake. Raw mode benefits from the same mapping without an AI call.
+    let mapped_transcript = terminology::apply(&transcript, &settings.terminology);
+
+    let refined_text = if mode.ai_cleanup {
         state::emit_status(app, Phase::Refining, mode_id);
-        refine_or_fall_back(app, &settings, &mode, &transcript).await
+        refine_or_fall_back(app, &settings, &mode, &mapped_transcript).await
     } else {
-        transcript.clone()
+        mapped_transcript
     };
+
+    // The AI is allowed to fix prose but not canonical product codes/names. Re-apply
+    // the deterministic layer immediately before delivery to enforce that invariant.
+    let final_text = terminology::apply(&refined_text, &settings.terminology);
diff --git a/src-tauri/src/stt/mod.rs b/src-tauri/src/stt/mod.rs
--- a/src-tauri/src/stt/mod.rs
+++ b/src-tauri/src/stt/mod.rs
@@
-use crate::settings::{SttProviderKind, SttSettings};
+use crate::settings::{SttProviderKind, SttSettings, TerminologySettings};
@@
 pub fn build_protocol(settings: &SttSettings, api_key: String) -> Box<dyn WsProtocol> {
+    build_protocol_with_terminology(settings, &TerminologySettings::default(), api_key)
+}
+
+pub fn build_protocol_with_terminology(
+    settings: &SttSettings,
+    terminology_settings: &TerminologySettings,
+    api_key: String,
+) -> Box<dyn WsProtocol> {
+    // Deepgram Nova-2 caps keywords at 100; using one shared capped list keeps provider
+    // switching deterministic and prevents an unexpectedly large websocket URL.
+    let terms = crate::terminology::stt_terms(terminology_settings, 100);
     match settings.provider {
-        SttProviderKind::Soniox => Box::new(soniox::Soniox::new(settings, api_key)),
-        SttProviderKind::Deepgram => Box::new(deepgram::Deepgram::new(settings, api_key)),
+        SttProviderKind::Soniox => Box::new(soniox::Soniox::with_terms(settings, api_key, terms)),
+        SttProviderKind::Deepgram => Box::new(deepgram::Deepgram::with_terms(settings, api_key, terms)),
         SttProviderKind::AssemblyAi => Box::new(assemblyai::AssemblyAi::new(api_key)),
     }
 }
diff --git a/src-tauri/src/session/mod.rs b/src-tauri/src/session/mod.rs
--- a/src-tauri/src/session/mod.rs
+++ b/src-tauri/src/session/mod.rs
@@
-    let protocol = stt::build_protocol(&settings.stt, api_key);
+    let protocol =
+        stt::build_protocol_with_terminology(&settings.stt, &settings.terminology, api_key);
     let stt_task = tauri::async_runtime::spawn(stt::run_stream(protocol, audio_rx, event_tx));
diff --git a/src-tauri/src/stt/soniox.rs b/src-tauri/src/stt/soniox.rs
--- a/src-tauri/src/stt/soniox.rs
+++ b/src-tauri/src/stt/soniox.rs
@@
 pub struct Soniox {
     api_key: String,
     model: String,
     language_hints: Vec<String>,
+    terms: Vec<String>,
 }
 
 impl Soniox {
     pub fn new(settings: &SttSettings, api_key: String) -> Self {
+        Self::with_terms(settings, api_key, Vec::new())
+    }
+
+    pub fn with_terms(settings: &SttSettings, api_key: String, terms: Vec<String>) -> Self {
         Self {
             api_key,
             model: settings.soniox_model.clone(),
             language_hints: settings.language_hints.clone(),
+            terms,
         }
     }
 }
@@
-        let config = json!({
+        let mut config = json!({
             "api_key": self.api_key,
@@
             "enable_endpoint_detection": true,
         });
+        if !self.terms.is_empty() {
+            config["context"] = json!({ "terms": &self.terms });
+        }
         Some(Message::Text(config.to_string()))
     }
diff --git a/src-tauri/src/stt/deepgram.rs b/src-tauri/src/stt/deepgram.rs
--- a/src-tauri/src/stt/deepgram.rs
+++ b/src-tauri/src/stt/deepgram.rs
@@
 pub struct Deepgram {
     api_key: String,
     model: String,
     language: String,
+    terms: Vec<String>,
 }
 
 impl Deepgram {
     pub fn new(settings: &SttSettings, api_key: String) -> Self {
+        Self::with_terms(settings, api_key, Vec::new())
+    }
+
+    pub fn with_terms(settings: &SttSettings, api_key: String, terms: Vec<String>) -> Self {
         Self {
             api_key,
             model: settings.deepgram_model.clone(),
             language: settings.language.clone(),
+            terms,
         }
     }
 
     fn url(&self) -> String {
-        format!(
+        let mut url = format!(
             "wss://api.deepgram.com/v1/listen\
@@
             language = urlencoding::encode(&self.language),
             rate = TARGET_SAMPLE_RATE,
-        )
+        );
+        for term in &self.terms {
+            // Nova-2 uses the legacy `keywords=term:intensifier` API. 1.5 is a modest
+            // boost: enough to help rare part codes without making false positives easy.
+            url.push_str("&keywords=");
+            url.push_str(&urlencoding::encode(term));
+            url.push_str(":1.5");
+        }
+        url
     }
 }
diff --git a/src/lib/types.ts b/src/lib/types.ts
--- a/src/lib/types.ts
+++ b/src/lib/types.ts
@@
 export interface HistorySettings {
@@
   audio_retention_days: number;
 }
+
+export interface TerminologyEntry {
+  canonical: string;
+  aliases: string[];
+  enabled: boolean;
+}
+
+export interface TerminologySettings {
+  enabled: boolean;
+  send_to_stt: boolean;
+  entries: TerminologyEntry[];
+}
 
 export interface AppSettings {
   stt: SttSettings;
+  terminology: TerminologySettings;
   llm: LlmSettings;
